# 贡献指南

本仓库是**结构规格 + 校验器**的开源 demo。它交付**结构**，不交付内容。

一句话前提：**你不需要向上游贡献任何东西，就能完整使用它。**
这份指南主要说明的是——**什么值得提回来，什么不该提回来。**

**提交入口（issue 与 PR）**：<https://github.com/aic-123/Scaffold/issues>

---

## 一、上游维护什么，不维护什么

| | 内容 |
|---|---|
| **上游维护** | 结构规格（[`SPEC.md`](SPEC.md)）、字段与规则（[`schema/`](schema/)、[`validator/`](validator/)）、接口契约（[`hooks/`](hooks/)）、文档 |
| **上游不维护** | 任何**真实领域内容** |

**请把你的领域知识放在你自己的 fork 或仓库里，不要提到上游。**

理由不是洁癖，是自洽：上游一旦开始收录真实内容，就必须决定"收录谁、不收录谁"——
而本项目的第一条设计原则是**权威度绝不进排序**。**收录本身就是一次权威背书。**

`samples/` 下那两个领域是**故意琐碎的演示**（路由器半夜断网、室友的泡面），
不是内容贡献的模板。请**不要**把它们"改进"成真实领域——它们故意这么小，
好让你一眼看清结构本身。

## 二、你不用问就能做的事

本设计是**零依赖、零锁定**，且**六层是能力上限，不是使用要求**。下面这些直接改就行，
不必提交回上游，也不必征得同意：

- **合并、裁剪、替换任何一层**——六层是能力矩阵，不是必填清单
- **增删字段**——改 `schema/node.schema.yaml` 与校验器里的镜像常量；
  跑 `--self-test` 会告诉你漏改了哪一处
- 换目录结构、换 id 分段、换文件名规则
- 把校验器接进你自己的 CI
- 写自己的填充器（别的模型、脚本、机构流程）——`filled_by` 的取值由你定

只要保住**四条硬规矩**（见 README）。扔了任何一条，这个仓库就没有存在的理由了。

## 三、欢迎提回上游的

1. **校验器的 bug**——尤其是**误报**（把合法结构判成错误）
2. **规格自相矛盾**——同一件事两处说法不一致
3. **文档与实际不符**——README / SPEC 说的和校验器实际做的不一样
4. **"能力声称 > 表达能力"**——文档说能表达某样东西，但**没有字段承载它**
5. **新增的规则靶子**——[`samples-broken/`](samples-broken/) 里一条规则一个文件

**第 3、4 条是本项目历史上出问题最多的地方，也最受欢迎。**
第 4 条尤其隐蔽：读代码看不出来，因为代码没错——是**文档比代码大**。

## 四、提之前先跑这三条

```bash
python validator/validate.py --self-test      # 内置自检
python validator/validate.py ./samples        # 应退出码 0（干净通过）
python validator/validate.py ./samples-broken # 应退出码 1（规则靶子全命中）
```

**PR 里请贴实际运行输出，不要只描述。**

本项目有一条硬要求：**文档里贴的输出块必须与实际输出逐行一致**。
所以你改了任何数据，要顺手把 README 里的**输出块 / 计数 / 示例名**一起 grep 一遍——
这三样过时过很多次了。

## 五、加规则的门槛比加字段高

**加字段**：改 schema + 校验器镜像，`--self-test` 会帮你查一致性。欢迎。

**加规则**：**请先开 issue 讨论，不要直接提 PR。** 三条理由，都是本项目踩出来的：

1. **规则会约束所有人**，包括只想用、不想改的人
2. **加规则会让既有使用者回头改自己的文件**——而"让人为了过校验改文件"正是本项目明确避免的
3. 规则一旦发出就是**对外承诺**，撤回来比加上去难

所以新增规则需要**设计方明确认账**。现成的例子：`R8` 是本仓库新增的第八条规则，
设计稿的七条里没有它，至今在 [`validator/rules.md`](validator/rules.md) 末节挂着"需要设计方认账"。

> **反过来，"我们不规定"也是一种决定。**
> 如果你发现某处没有规则、而且希望它**保持没有**，同样欢迎提。
> README 里已经有两处这样的明确声明（案例正文形状、案例 `relations` 方向）——
> 见「我们不规定什么」。**没限制也要写清楚**，否则使用者会以为有规矩。

## 六、提交格式

没有模板，不要求签名。但请做到：

- **一次提交说一件事。** 本项目的版本审计全靠 diff 读得懂，混在一起的提交读不了
- **改了数据就回头 grep 文档**——输出块、计数、示例名，三样都要对
- **不提交行尾变化。** 仓库是 LF，[`.gitattributes`](.gitattributes) 已钉死。
  若你的编辑器写回 CRLF，`git diff` 会显示**整文件改动**，把真正的改动淹掉
- **不要提交真实领域内容**（见第一节）
- 提交信息用中文或英文都行，仓库中英双语

## 七、维护节奏

**best-effort。** 每月集中处理一次 issue 与 PR。

没有人有义务及时回复，也没有人能用"我提了"要求什么。这个项目刻意不承诺 SLA。

---

*贡献指南 v0（2026-09）。*

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->
