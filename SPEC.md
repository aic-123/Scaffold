# SPEC.md · 结构规格 v0

本文件只定义**结构**：字段有哪些、六层各装什么、两个索引怎么分工。
**它不定义任何内容。** 所有内容由人工填充，本规格不提供、不推断、不补全。

字段清单的机器可读版本在 [`schema/node.schema.yaml`](schema/node.schema.yaml)。
两份定义必须一致，可用 `python validator/validate.py --self-test` 校验。

**关于"编辑器"**：设计稿的交付物写作"结构校验器/编辑器"。本仓库交付的是**校验器**；
"编辑"这一环**不需要专门工具**——一个节点就是一个 Markdown 文件，任何文本编辑器都能改。
这正是"零依赖零锁定"的用意所在，因此不提供图形编辑器，也不计划提供。

---

## 一、节点字段

一个节点 = 一个 Markdown 文件 = YAML front matter + 可选正文。

```yaml
---
id: "con-0001"                           # 类型前缀-四位序号，全局唯一（见下）
type: 概念 | 判断点 | 条件 | 例外 | 议题 | 立场 | 论据 | 断言 | 案例
title: "..."                             # 必填，概念索引的键
aliases: ["别名1", "别名2"]               # 词汇层别名（SKOS altLabel）。空数组合法，不报警
cues: ["症状/线索1", "症状/线索2"]        # 必填，情境索引（调用的真正入口）
scope: "适用范围（含不适用）"              # 必填
source:                                  # 必填字段，但内部可为空
  ref: "DOI / 标准编号 / 教材章节 / 链接"
  kind: 教材 | 论文 | 标准 | 官方文档 | 个人 | 未知
evidence_status: 已确立 | 假说 | 有争议 | 已淘汰 | 未验证
relations: []                            # 指向其他节点 id，含"例外覆盖"
filled_by: 人 | 模型(vX) | 未填充          # 取值是约定，不是白名单——校验器不校验其内容（见下）
notes: ""
---
正文（可选）。判断点必须写清"看哪几个变量（≤4个）"；分歧层写立场与论据。
```

共 11 个字段，**不得增删、不得改名**。字段含义见 `validator/rules.md` 与 schema 文件。

> **`aliases` 与 `cues` 严格区分，混用会让情境索引失效：**
> `cues` 答"我现在处在哪种处境"（症状、线索、现场特征）；
> `aliases` 答"这东西还叫什么名字"（同义词、俗称、错称）。
> 前者是调用入口，后者是词汇层的 altLabel。

> **`filled_by` 的取值不设限制，这是有意的。**
> 上面的 `人 | 模型(vX)` 是本仓库的**约定，不是白名单**——校验器只要求该字段存在，不校验其内容。
> 下游要接自己的填充器（别的模型、脚本、机构流程），自行定义取值、在自己的校验里加约束即可，
> 不必改这份结构。这里只保证一件事：**填了就得留痕，且留的痕要能看出是谁填的。**

> **唯一的例外是 `未填充`——它有结构含义，受规则 R8 管。**
> 它不是一种署名方式，而是一个**事实断言**："这个节点还没人填过"。
> 既然是个断言，它就能被校验：**声明了 `未填充` 的节点里不能有任何内容**
> （`title`、`scope`、`notes`、`aliases`、`cues`、`relations`、`source.ref`、正文，任一非空即算；
> `source.kind` 写 `未知` 不算），否则报 `R8`。
>
> 这条是**立项根基第 2 条（填充者留痕）的兜底**。允许 `未填充` 与内容并存，
> 等于给"填了但不署名"开了一条合法通道——那样"留痕"就只剩 README 里的一句自觉。
> R8 是**本仓库新增的第八条规则**，设计稿七条里没有，需要设计方明确认账或否掉
> （见 [`validator/rules.md`](validator/rules.md) 末节）。

### id 前缀映射

| type | 前缀 | 例 |
|---|---|---|
| 概念 | `con` | `con-0001` |
| 判断点 | `judge` | `judge-0001` |
| 条件 | `cond` | `cond-0001` |
| 例外 | `exc` | `exc-0001` |
| 议题 | `issue` | `issue-0001` |
| 立场 | `stance` | `stance-0001` |
| 论据 | `arg` | `arg-0001` |
| 断言 | `claim` | `claim-0001` |
| 案例 | `case` | `case-0001` |

> 设计稿只给了示例 `term-0001`，未定义 9 种 type 的前缀。本表是执行时补的，
> **已确认采用英文短前缀**（`con` / `judge` / `cond` / `exc` / `issue` / `stance` / `arg` / `claim` / `case`）。
> 设计稿原示例的 `term-` 已统一为 `con-`，与其余 8 个前缀保持同一风格，避免两套写法并存。

### id 唯一性：已定「全局唯一」

`id` 采用**全局唯一**。理由：`relations` 是纯 id 引用，且可以跨目录、跨领域。
若只要求"领域内唯一"，同一个 `con-0001` 在两个领域会指向不同东西，引用立刻产生歧义。

代价是格式被固定为「类型前缀-四位序号」，多领域并存时会正面撞车——两边都想从 `con-0001` 开始编号。
因此约定：**一个领域占一个编号段。**

| 领域 | 编号段 |
|---|---|
| `samples/router-reset` | `0001` – `0099` |
| `samples/roommate-noodles` | `0101` – `0199` |
| `samples-broken`（校验器靶子，非知识） | `9001` – `9099` |

即 `con-0001` 与 `con-0101` 是两个不同领域的同名概念。新增领域顺延下一个百位段。

> ⚠️ **这个约定的上限是 99 个领域 × 每领域 99 个节点**（百位段最多排到 `9901`–`9999`）。
> demo 够用，真实使用会撞墙。超出时的扩展路径（按代价从低到高）：
> ① 放宽序号位数（如 `con-00001`）② 领域缩写进 id（如 `router-con-0001`）
> ③ 改为「领域内唯一 + 跨领域用路径引用」。
> 三条都会动到格式或校验器，属于第二步之后的议题——**这里只登记，不预做**。

### relations 的方向约定

`relations` 是**有向**的：A 出现在 B 的 `relations` 里，表示「A 指向 B」，**不代表 B 也指向 A**。

双向写会立刻构成环，被 R3 判为循环依赖——这是设计使然，不是 bug。
`立场 ↔ 论据` 这种天然的互相引用，只写一个方向，反方向用正文里的文字引用（「支撑论据见 arg-0001」）。

本仓库约定的方向：

| 关系 | 方向 | 谁写进 relations |
|---|---|---|
| 判断点的前置条件 | 判断点 → 条件 | 判断点 |
| 例外覆盖 | **例外 → 被覆盖的节点** | 例外 |
| 立场归属 | 立场 → 议题 | 立场 |
| 论据支撑 | **论据 → 立场** | 论据 |
| 概念关联 | 由细 → 由粗 | 任意一方，单向 |

> 设计稿只说 `relations` 是「指向其他节点 id，含例外覆盖」，未定方向。**已确认采用上表约定。**
> 理由：不定方向，`立场 ↔ 论据` 这类天然互引必然被写成双向、构成环、被 R3 误报为错误。
> 单向写 + 反向用正文文字引用，是唯一不会触发误报的解法。

**表里没有 `案例`——这是有意的，不是漏写。** 案例指向谁、朝哪个方向指，
本规格**不作约定**，由使用者自定。理由：案例正文的形状本来就没被校验（见 §二），
再给它定一个方向，等于规定一件没人检查、却要人改文件去遵守的事。
校验器对案例的 `relations` 只查两件事：**引用的 id 存不存在**、**有没有成环**（R3）。

> ⚠️ 代价说在明处：`samples/` 里 `case-0001` 写着 `relations: ["con-0001"]`、
> `case-0101` 写着 `["judge-0101", "case-0001"]`——**这只是这两个示例自己的写法，不是约定**。
> 你反着指、指向别的类型，校验器都不会说话。
> **没有限制，但也没有保障。**

### evidence_status 的两类值

| 类别 | 值 | 谁能写 |
|---|---|---|
| 用户可写 | `已确立` / `假说` / `有争议` / `已淘汰` / `未验证` | 任何人 |
| 仅钩子可授予 | `已验证` | **只有验证方**，经 `hooks/` 契约写入 |

```yaml
# 默认值，缺省即此
evidence_status: 未验证
```

`已验证` **不在**用户可写集合内。任何样本文件或手写文件里出现它，校验器一律报 ERROR（规则 R7）。
唯一例外是 `--allow-verified`，且该开关只允许用于 hooks 授权的输出目录。

> 设计稿的字段枚举列的是**用户可写值**（5 个，不含 `已验证`）；而硬规矩 3 与 §2 表格管的是**授予权限**
> （"只有验证方能写该字段"）。两件事各管一摊，并不矛盾。
>
> **已确认**：`已验证` 是规格内合法存在的状态，但不属于用户可写集合——
> 即「**能存在于文件中的值有 6 个，用户能写的只有 5 个**」。
> 这个区分必须在校验器、schema、文档三处表达一致，不得简化成"合法值只有 5 个"。

---

## 二、六层：能力矩阵，不是必填清单

**第一阶段只要求 ①② 层（词汇层、结构层）落地。** ③④⑤⑥ 是"需要表达时再用"的可选表达力。
六层是能力上限，不是使用要求；你可以合并、裁剪、替换任何一层，只要保持四条硬规矩
（不代填 / `filled_by` 如实 / `evidence_status` 不接受自声明 / 权威度不进排序）。

| 层 | 名称 | 装什么 | 借用的标准形状 | 对应 type | 第一阶段 |
|---|---|---|---|---|---|
| ① | **词汇层** | 术语表、别名、多语言（同一概念别三种叫法）⚠️ 多语言仍缺，见下 | SKOS 式（轻量） | `概念` | **必须落地** |
| ② | **结构层** | 判断点、条件、阈值、例外、依赖关系 | DMN 决策表式 | `判断点` `条件` `例外` | **必须落地** |
| ③ | **分歧层** | 议题-立场-论据，**不收敛** | IBIS / Toulmin | `议题` `立场` `论据` | 待定 |
| ④ | **溯源层** | 断言 + 出处 + 证据状态 | nanopublication 三段式 | `断言` ⚠️推断 | 待定 |
| ⑤ | **案例层** | 情境特征 + 考虑过的选项 + 理由 + 结果 + 反事实 | CBR 案例 schema（第二阶段） | `案例` ⚠️推断 | 待定 |
| ⑥ | **视图层** | 查询/排序规格（可见、可改、可版本化） | 自定义 SortSpec | 无——不是节点 | 待定 |

> ⚠️ **④⑤⑥ 的"对应 type"一列是执行时按形状推断的**，设计稿只给了层的名称与内容，
> 未指定 type 归属（设计稿 §1 只说 `type` 有九种，未说明每种归属哪一层）。
> 上表 ①②③ 的对应关系是设计稿隐含的，④⑤⑥ 的是本仓库补的，**需要设计方确认**。
> ⑥ 视图层装的是查询规格（SortSpec），不是知识节点，
> 因此它很可能根本不该有 `type`——这一条尤其需要确认。

> **⑤ 案例层那五段是形状描述，不是要求。** 上表列「情境特征 + 考虑过的选项 + 理由 +
> 结果 + 反事实」，但**只有情境特征有字段承载**（落在 `cues`）；其余四段只能写进正文，
> 而正文是自由的。**本规格不对案例正文作任何规定**——写几段、怎么写、写不写，都由使用者定。
>
> 因此 `type: 案例` 保证的只是"它自称是案例"，**不保证它有案例的形状**。
> 实测：正文完全空着的 `案例` 节点照样通过校验（退出码 `0`，零报告）。
>
> **这是有意的，不打算补规则。** 补一条"案例正文必须出现这四段"，等于让人为了过校验
> 回头去改自己的文件——而这一层本来就是可选的表达力。代价是校验器不会告诉你
> 案例写全了没有。使用者需要知道这一点，所以同时写进了 README 的「我们不规定什么」。

### 词汇层缺口：已解决（记录来龙去脉）

**原缺口**：① 词汇层声称装"术语表、**别名**、**多语言**（同一概念别三种叫法）"，
且该层是第一阶段**必须落地**的两层之一；但节点字段里只有 `title` 一个位置能放名字，
**没有任何字段承载别名或多语言**。

后果是"同一概念别三种叫法"这个能力**无法表达**——`con-0001` 只能叫"假死"，
想加"假掉线""假连接"作为别名，无处可写。词汇层等于只做了一半。

**已确认的解法（方案 A）：**

| | 内容 |
|---|---|
| **改动** | 节点字段新增 `aliases: []`（字符串数组），字段总数由 10 变 11 |
| **语义** | SKOS 的 **altLabel**：同义词、俗称、错称 |
| **索引** | 概念索引由 `title` 扩展为 `title` + `aliases` |
| **空值** | `aliases: []` 合法且**不报警**——与 `cues` 不同，不是每个概念都有别名 |
| **示例** | `samples/` 中 4 个概念节点已填别名演示，其余 19 个节点留空 |

> `aliases` 与 `cues` 必须严格区分，混用会让情境索引失效：
> `cues` 是"当事人怎么描述自己的**处境**"，`aliases` 是"这个东西**还叫什么名字**"。
> 这正是 R6 存在的理由。

**仍未覆盖**：**多语言标签**（同一概念的其他语言写法）。
`aliases` 只承载同一语言内的别名；真正的多语言需要另加字段或改成带语言标签的结构——
**属于后续议题，此处只登记，不预做。**

③④⑤⑥ 都属于"可选表达力"：第一阶段不要求落地，但**它们的定义是明确的**，
不存在"未被命名"的层。`samples/` 里已按 ③ 分歧层与 ⑤ 案例层的形状各留了示例：
③ 是「议题 + 两个对立立场 + 各自论据」，⑤ 是两个案例节点（`case-0001` / `case-0101`）。

> ⑤ 那两个案例的 `结果` **一律空着，且是故意的**。案例层要演示的不是"案例写得多完整"，
> 而是**空值怎么拦住一次错误判断**：案例能被 `cues` 检索到，但因为 `结果` 空着，
> 检索到它的人拿不到"该怎么办"。这两件事必须分开——**能被检索到，不等于能被照抄**。
> 两个案例互指（`case-0101 → case-0001`）——**这是这两个示例自己的选择，不是约定**
> （见 §一：案例的 `relations` 方向本规格不作规定）。互指的理由不是处境相似，
> 而是**它们空的是同一个字段**。

> 注：`node.schema.yaml` 里**没有** layer 字段。层是组织方式，不是节点属性——
> 一个 `论据` 既可以出现在分歧层，也可以出现在别处。校验器因此不对"层"做任何检查。

---

## 三、双索引

每个节点被两个索引同时挂住：

| 索引 | 键 | 回答的问题 | 典型读者动作 |
|---|---|---|---|
| 概念索引 | `title` + `aliases` + `type` | 这东西叫什么 | 已知名字（含俗称、错称），来查定义 |
| 情境索引 | `cues` | 我现在处在哪种处境 | 不知道叫什么，只知道自己遇到了什么 |

> 概念索引问"这东西叫什么"；情境索引问"我现在处在哪种处境"。**后者才是调用入口**，因为专家靠"认局"触发知识，不靠概念树。

这条是硬约束，不是风格偏好：`cues` 必填、`cues` 为空报 WARN（R6），
而 `title` 缺失不影响"能否被叫出来"以外的事。

`cues` 写的是**症状、线索、现场特征**，不是同义词、不是关键词、不是标签。
判断"写对了没有"的标准只有一条：**一个还不知道这个名字的人，会不会用这句话描述自己的处境。**

### scope：边界和内容同等重要

`scope` 必填，且必须同时写清**适用**与**不适用**。只写适用范围的节点，等于把误用风险留给读者。
`scope` 缺失或为空不报错，但一个写不出边界的节点，通常说明词汇层还没想清楚。

---

## 四、与既有标准的关系

本设计借用了 SKOS、DMN、IBIS、nanopublication 的**形状**，因为人类读得懂：
`概念` 对应 SKOS 的 concept，`判断点` 对应 DMN 的 decision table 的输入输出形状，
`议题 / 立场 / 论据` 对应 IBIS 的三元结构，`source` + `evidence_status` 对应 nanopublication 的
assertion / provenance 分层。借形状是为了让人一眼认出来，不是为了兼容。

**不实现**它们的完整规范：不做推理机、不做一致性检查、不做 OWL、不导出任何标准序列化格式。

差异的根因是**约束方向相反**。开放世界假设要"推出你想要的"——不知道的先当作存在，
由推理补全；本设计要"不知道就留空"，空值是合法且必须保留的状态。
一致性要求要"消除矛盾"；本设计的**分歧层要保留矛盾**——议题和立场不是待清理的脏数据，
是知识尚未收敛时的真实形状。一个把矛盾抹平的校验器，在这里是坏掉的校验器。

---

*结构规格 v0（2026-09）。内容由人工补充。*

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->
